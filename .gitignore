.env
.env.local
# Demo wallet private keys. Testnet-only, but a repo is the wrong place for a key.
wallets.json
node_modules/
contracts/out/
contracts/cache/
contracts/broadcast/
web/.next/
web/node_modules/
web/out/
*.wtns

web/.vercel/

# Holder secrets that open a commitment. The redacted index is notes.public.json.
notes.json

# Internal handoff notes - contain sandbox credentials.
docs/HANDOFF.md
# Cleanverse's own documentation is access-code gated; not ours to republish.
docs/docs-cleanverse.txt

# Proof artifacts carry the same opening secrets as notes.json.
.artifacts/
contracts/.artifacts/

# The raw association set carries other teams' wallets; asp.public.json is published.
asp.json
asp.previous.json
